@model HealthHub.Models.ViewModel.MedCardVM

<div class="conteiner p-4 border">

@*Закоментировал т.к. нехрен юзеру менять свой профиль. Пусть єтим занимается врач. А там, в будующем, возмонжно, я передумаю :)*@
@*    <div class="row pb-2"> 
        <div class="col">
            <h1 class="text-primary">Profile</h1>
        </div>
        @if (Model.medCardDTO != null)
        {
            <div class="col text-end pt-1">
                <a asp-controller="DoctorProfile" asp-action="Upsert" class="btn btn-success"> <i class="bi bi-plus-square"></i> Change</a>
            </div>
        }
        else
        {
            <div class="col text-end pt-1">
                <a asp-controller="DoctorProfile" asp-action="Upsert" class="btn btn-success"><i class="bi bi-plus-square"></i> Fill out your profile</a>
            </div>
        }
    </div>*@

    <br /><br />

    @*Вместо тблицы просто вставить шаблон html из upsert (Возможно, так будет только для доктора)*@
    @if (Model.medCardDTO != null)
    {
        <div class="row">
            <div class="col-8" enctype="multipart/form-data">
                <div class="form-group row py-2">
                    <div class="col-4">
                        <label asp-for="@Model.appUserDTO.Name"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="@Model.appUserDTO.Name" class="form-control" disabled />
                        <span asp-validation-for="@Model.appUserDTO.Name" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-group row py-2">
                    <div class="col-4">
                        <label asp-for="@Model.appUserDTO.Surname"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="@Model.appUserDTO.Surname" class="form-control" disabled />
                        <span asp-validation-for="@Model.appUserDTO.Surname" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-group row py-2">
                    <div class="col-4">
                        <label asp-for="@Model.appUserDTO.Email"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="@Model.appUserDTO.Email" class="form-control" disabled />
                        <span asp-validation-for="@Model.appUserDTO.Email" class="text-danger"></span>
                    </div>
                </div>

                <div class="form-group row py-2">
                    <div class="col-4">
                        <label asp-for="@Model.appUserDTO.Birthday"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="@Model.appUserDTO.Birthday" class="form-control" type="date" disabled />
                        <span asp-validation-for="@Model.appUserDTO.Birthday" class="text-danger"></span>
                    </div>
                </div>
                <div class="form-group row py-2">
                    <div class="col-4">
                        <label asp-for="@Model.medCardDTO.Desc"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="@Model.medCardDTO.Desc" class="form-control" disabled />
                        <span asp-validation-for="@Model.medCardDTO.Desc" class="text-danger"></span>
                    </div>
                </div>
            </div>
            @*Потом, если будет не лень, сделаю вывод фотки врача*@
@*            <div class="col-4">
                @if (Model.medCardDTO != null)
                {
                    <img src="@WC.ImagePath@Model.medCardDTO.PathToPhoto" width="100%" style="border-radius:5px; border:1px solid #bbb" />
                }
            </div>*@
        </div>
    }
    else
    {
        <p>MedCard is not filled out</p>
        <p>Only a doctor can fill out a medical card</p>
    }

</div>