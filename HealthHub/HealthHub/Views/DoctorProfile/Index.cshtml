@model HealthHub.Models.ViewModel.DoctorProfileVM

<div class="conteiner p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">Profile</h1>
        </div>
        @if (Model != null)
        {
            <div class="col text-end pt-1">
                <a asp-controller="DoctorProfile" asp-action="Upsert" class="btn btn-success"><i class="bi bi-plus-square"></i> Change</a>
            </div>
        }
        else
        {
            <div class="col text-end pt-1">
                <a asp-controller="DoctorProfile" asp-action="Upsert" class="btn btn-success"><i class="bi bi-plus-square"></i> Fill out your profile</a>
            </div>
        }
    </div>

    <br /><br />

    @*Вместо тблицы просто вставить шаблон html из upsert (Возможно, так будет только для доктора)*@
    @if (Model != null)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <tread>
                <tr>
                    <th>
                        Topic Name
                    </th>
                    <th>
                        Sectioc Type
                    </th>
                    <th>
                        Description
                    </th>
                    <th>
                        Create Time
                    </th>
                    <th>
                        Last Change Time
                    </th>
                    <th></th>
                </tr>
            </tread>
            <tbody>
                @*                @foreach (var obj in Model)
            {
            <tr>
            <td wight="15%">@obj.Name</td>
            <td wight="15%">@obj.Section.Name</td>
            <td wight="20%">@obj.Description</td>
            <td wight="20%">@obj.CreateTime</td>
            <td wight="20%">@obj.LastChangeTime</td>
            <td class="text-center">
            <div class="w-55 btn-group" role="group">
            <a asp-controller="Topic" asp-route-Id="@obj.Id" asp-action="Upsert" class="btn btn-primary mx-2">
            <i class="bi bi-pencil-square"></i>
            </a>
            <a asp-controller="Topic" asp-route-Id="@obj.Id" asp-action="Delete" class="btn btn-danger mx-2">
            <i class="bi bi-trash3"></i>
            </a>
            </div>
            </td>
            </tr>
            }*@
            </tbody>
        </table>
    }
    else
    {
        <p>Profile is not filled out</p>
    }

</div>